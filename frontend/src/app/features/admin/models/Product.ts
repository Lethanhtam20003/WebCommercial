export interface Product {
  name: string;
  price: number;
  discountPrice: number;
  category: string;
  imageUrl: string;
  inStock: boolean;
  isHot: boolean;
  isNew: boolean;
  isPromo: boolean;
  visible: boolean;
  order: number;
}

