export interface Product {
  imageUrl: string;
  name: string;
  price: number;
  discountPrice: number;
  category: string;
  inStock: boolean;
  isHot: boolean;
  isNew: boolean;
  isPromo: boolean;
  visible: boolean;
  order: number;
}

